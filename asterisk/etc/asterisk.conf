; Hermes: Asterisk core options.
; German ring/busy tones come from indications.conf; Hermes' own announcements
; are played by path, so no sound language is set here.
[options]
; Nothing verbose in the container log (it would contain phone numbers).
; For live debugging: docker compose exec asterisk asterisk -rvvv
verbose = 0
