; Hermes: warnings and errors go to the container log (`docker compose logs asterisk`).
; For live call debugging: docker compose exec asterisk asterisk -rvvv
[general]
dateformat = %F %T

[logfiles]
console => notice,warning,error
