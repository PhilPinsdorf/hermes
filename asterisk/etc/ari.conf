; Hermes: the Phoenix app controls calls through ARI.
[general]
enabled = yes
pretty = no
; No browser origins: ARI is only used server-to-server.
allowed_origins =

[${ARI_USER}]
type = user
read_only = no
password = ${ARI_PASSWORD}
password_format = plain
